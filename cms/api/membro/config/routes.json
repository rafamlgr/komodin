{
  "routes": [
    {
      "method": "GET",
      "path": "/membros",
      "handler": "Membro.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/membros/count",
      "handler": "Membro.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/membros/:_id",
      "handler": "Membro.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/membros",
      "handler": "Membro.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/membros/:_id",
      "handler": "Membro.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/membros/:_id",
      "handler": "Membro.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
